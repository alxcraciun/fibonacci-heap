{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "clang++.exe - Build and debug active file",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}\\main.exe",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
        }
    ]
}